<div
  class="uc-add-to-quotes"
  data-product-id="{{ product.id }}"
  data-variant-id="{{ product.selected_or_first_available_variant.id }}"
  data-product-handle="{{ product.handle }}"
></div>

{% schema %}
{
  "name": "Uncap Quotes Product",
  "target": "section"
}
{% endschema %} 